#!/usr/bin/env php
<?php

set_time_limit(0);

require __DIR__ . '/../vendor/autoload.php';

use Cherry\Console\Console;
use Cherry\Kernel;

$app = new Kernel(dirname(__DIR__));

$console = new Console();
